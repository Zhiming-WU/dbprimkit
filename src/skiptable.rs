//! Not implemented.

/*use std::{ptr::NonNull, sync::atomic::AtomicPtr};

pub struct SkipTable<K, V> {
    root: Option<NonNull<Node<K, V>>>,
    max_hight: u8,
}

struct Node<K, V> {
    key: K,
    value: V,
    height: u8,
    next: [AtomicPtr<Node<K, V>>],
}

impl<K, V> SkipTable<K, V> {
    pub fn new() -> Self<K, V> {
        SkipTable<K,V> { root: None }
    }
}*/
